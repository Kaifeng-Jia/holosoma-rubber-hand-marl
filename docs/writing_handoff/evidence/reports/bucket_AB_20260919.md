# 桶交接 A/B：最终12000轮模型的首次本地评测

日期：2026-09-19 UTC。只整理、评测与回放，未改训练代码/奖励、未续训、未删除文件。

## 结论

两组都完成12000轮训练。固定场景同协议各5回合，A有2回合执行到参考末尾，B有1回合。
B在示范接触时段内的手—桶接触覆盖更高，但本次没有体现更高的完整执行率。
两组完整回放均有持续约4秒的桶网格离地，区别于此前小桌基本未持续抬升。
这些结果不能直接宣称双手承重、正确释放/交接或双方必要贡献已实现。

## 直接观看

- A：<http://localhost:8080>，本地训练最终模型，episode_004。
- B：<http://localhost:8081>，云端训练最终模型在本地的物理回放，episode_002。
- 两者均使用训练橡胶手URDF和原尺寸1kg桶资产，50fps，关节顺序已核对一致。
- 选择规则：优先完整回合，再选物体位置RMSE最小者；不是随机抽样，不代表平均质量。
  全部10回合回放均保留在各自evaluation目录，失败没有裁掉或隐藏。

## 训练身份及收件

A：`../A_fresh12000_env2048_seed721/`；B：`../B_fresh12000_env2048_seed721_cloud_20260918/`。
两组均fresh seed721、2048环境×24步、12000轮、每2000保存，Actor158/Critic527。
配置与物理参数相同，奖励契约仅variant A/B不同。非文档源码及资产与冻结云端清单一致。
云端实际GPU为RTX4090，本地RTX5070 Laptop；单训练seed、跨GPU，是探索性对照。

- A连续12000条有限指标、最终完整模型均已验证；纯迭代累计14.874小时。
- B连续12000条有限指标、最终完整模型均已验证；纯迭代累计9.065小时。
- A保存初始及2000至12000的7个checkpoint。本次B只收到最终模型，Mac仍有全部7个的备份
  （云端交接说明所述，Ubuntu未独立检查Mac文件）。不能把B中间模型记成已在本地。
- 两层归档校验及包内18项SHA256全部通过。ZIP原件保留；导入映射和本地SHA清单在B目录。
- 云端原始JSON/启动脚本/环境说明保留于B的`cloud_handoff/`，没有执行附带脚本。
- A训练服务已消失，journal仅有启动与CPU耗时记录；**实际最终进程退出码未能取回，记未知**。
  不用不存在服务的默认ExecMainStatus=0补填。B的退出码0由原Popen监督记录提供。
- 本轮A、B本地评测进程均实际返回0，并各生成summary及5份完整记录，日志无Traceback。

## 同协议逐回合结果

执行Actor和其归一化器，Critic不参与动作生成；seed721，无观测噪声，各5回合。
初始化沿既有评测入口，录制从参考frame1开始，最终frame496；496控制步为完整回合。
“完成”只指原reference_horizon条件，不是新增的交接语义成功判定；失败均为joint_bad_tracking。

| 组别/回合（0起算） | 控制步 | 到达参考末尾 | 物体3D RMSE(cm) | 物体原点z RMSE(cm) |
| --- | ---: | --- | ---: | ---: |
| A0 | 245 | 否 | 6.89 | 2.70 |
| A1 | 414 | 否 | 10.77 | 2.72 |
| A2 | 496 | 是 | 11.26 | 2.38 |
| A3 | 304 | 否 | 5.78 | 2.25 |
| A4（展示） | 496 | 是 | 6.69 | 1.65 |
| B0 | 492 | 否 | 12.35 | 2.30 |
| B1 | 395 | 否 | 8.64 | 2.69 |
| B2（展示） | 496 | 是 | 8.63 | 2.08 |
| B3 | 314 | 否 | 6.84 | 1.96 |
| B4 | 392 | 否 | 7.18 | 2.27 |

失败回合误差仅覆盖已执行前缀，不能与完整回合混合归因。5次重复不是5个独立训练seed，
2/5和1/5不足以认定A在一般意义上优于B；也不因B有一回合接近末尾就改完成标准。

## 接触与抬升：各自选优完整回放

接触定义保持训练一致：任一手—桶法向力模长>1N；用原始示范软标签alpha加权统计覆盖率。
初始无物理采样状态排除；按保存的reference_frame对齐，不将回放行号冒充参考帧。

| 指标 | A4 | B2 |
| --- | ---: | ---: |
| 人1示范接触时段加权覆盖 | 71.2% | 97.8% |
| 人2示范接触时段加权覆盖 | 68.3% | 100.0% |
| 桶源网格最低点>1cm的最长连续段 | 3.88s | 4.00s |
| 桶源网格最低点最高位置 | 34.3cm | 36.2cm |

相同算法下参考连续离地约4.14s、最低点最高约30.8cm。这里使用原始mesh顶点和实际物体
位姿估计离地，不是PhysX烘焙凸包逐点测量；法向力也不是包含摩擦的总承重测量。
不能只因手碰到了桶、桶离地，就断言它只靠手提起。

B在交接后的参考279–496区间，人1仍约51.6%的有效样本与桶有手接触；额外接触本来不被
B奖励惩罚。因此“要求接触时有接触”改善，不等于正确释放或独立接收已经实现，需看画面。

训练末100轮的缺失接触误差A约0.2945、B约0.0348，B调制因子约0.9834；这支持接触调制
产生影响，但训练随机参考后缀的分布与从头评测不同。关系正奖励均值A约0.0129、B约0.0115，
仍很低，未据此开展根因判断或改变尺度；不把不同定义的训练总reward直接排名。

## 后续边界

先由用户观看8080/8081，判断抬起、交接、释放和身体动作是否符合预期。
当前不追加训练、不改奖励、不挑中间模型来替换最终模型。若需比较B的早期checkpoint，
再向Mac索取已有备份。参与贡献与额外身体接触仍需独立诊断，不能凭以上力阈值下结论。

详细数据：`ANALYSIS.json`；可复算脚本：`reproduce_analysis.py`（hssim Python，无仿真）。
两组各自`evaluation_20260919_seed721/summary.json`、`episode_000`至`episode_004.npz`
及`representative_episode.npz`为原始评测产物。
