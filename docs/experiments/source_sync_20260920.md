# 2026-09-20 研究源码、测试与小型资产同步

用户在文档提交 `70a3c9a5` 后明确授权提交其余研究内容。本次仍在 `core4d-base`，
不合并官方main、不重新训练、不修改当前物理或奖励参数，不上传环境/凭据/模型。

## 提交范围

- 桶A/B、单项与双删除奖励配方，参考相对向量、手—物体法向接触采样、模型身份与诊断。
- 5kg小桌独立资产/实验入口、完整A与双删除支持；保留旧20kg默认与原实验。
- 独立高度惩罚及诊断、统一Actor-only评测、失败终态/高度/接触回放记录。
- CORE4D原始数据预览、共享缩放参考导出及A1腕部处理；默认关闭的PT掌面避碰修正候选。
- 椅子、桶、5kg小桌所需小型参考、物体URDF/mesh、来源与资产清单，共23个文件约16MB。
- 配套测试、桶训练配置说明、现有story及A1说明更新、历史LongTermGoal定位、人工选片笔记seedata.txt。

提交前选定45个Python文件、23个资产文件与5个既有说明/笔记文件，共17434944字节；
另追加本同步说明并更新在线评测报告的提交范围说明。没有纳入checkpoint或完整数据集。
数据来源/来源绝对路径保持溯源，不改写冻结资产字节或SHA；源数据许可及模型依赖仍按原项目和数据源要求处理。

未提交：`logs/`、训练checkpoint/回放、Isaac生成缓存、两套Ubuntu环境审计目录及压缩包、
`发给Ubuntu_桶交接B.zip`、Python/Conda环境、SSH私钥与凭据。未删除这些本地文件。
已有小型参考可支持对应训练入口；重新从原始数据制作参考仍需另行准备数据集及现有依赖环境。

## 已执行验证

- 训练/配置/奖励/模型身份相关10个测试文件：277 passed。
- 重定向、预览、参考导出、椅子碰撞及向量审计相关7个测试文件：94 passed。
- 合计371 passed；全部选定Python文件AST语法检查、代码及文档的Git diff空白检查通过。
- 两个源OBJ文件末尾各有额外空行，Git报告空白提示；为保持冻结mesh字节与SHA一致，原样保留，不作格式化。
- 选定文件进行了常见凭据模式筛查，未匹配私钥、GitHub token或带密码的HTTP URL。

测试环境没有安装/升级包：训练测试使用hssim。hsretargeting没有pytest，hssim没有MuJoCo；
先前两次直接调用因这两个环境差异未能执行对应测试，随后在hsretargeting进程中预加载本环境
NumPy/SciPy/MuJoCo/CVXPY，再将hssim的site-packages追加到sys.path末尾，仅临时复用已有pytest，
关闭第三方pytest插件自动加载。该操作不修改持久环境或依赖版本。

94项测试出现4941条警告：4940条为既有NumPy数组转标量弃用提示，一条为CLARABEL数值精度提示。
测试断言均通过；本次不借发布任务修改求解器或追求消除警告，也不声称已经做完整仓库全量测试。
刚完成的两组小桌12000训练与本地各5回合物理评测另见[结果报告](core4d_smalltable_5kg_20260920.md)。

## 状态与复现边界

PT掌面避碰是历史可选实验的存档，不是恢复被暂停的OMOMO训练，不取代已认可的A1默认路径。
本提交将当前研究工作树源码整理入库；具体云端训练使用冻结overlay，其逐文件SHA清单已随结果
保留在本地 `cloud_provenance/package_manifest.json`。因此不把本提交号冒称为原始训练启动时的提交号。
所有新训练、续训、改奖励或扩展方向仍按主路线和用户后续确认执行。
