# CORE4D Raw Audit：20231003_2/000

## 结论

`20231003_2/000` 可以作为 CORE4D-Base 的首个真实样本。它是官方标注的 `move2_obs0` 双人无障碍搬运动作，物体为 `desk001`。V1 两个人体、物体轨迹和对齐表均为 197 帧，数值与旋转校验通过；本阶段没有运行 OmniRetarget，也没有训练策略。

## 来源与范围

- 动作标签：`move2_obs0`
- `move2`：两名参与者都知道移动目标；`obs0`：无障碍
- 采样率：15 FPS
- 样本数：197；从 frame 0 到 frame 196 的时间跨度为约 13.067 秒
- 物体：`desk001`
- 下载方法：对官方 Hugging Face ZIP 使用 HTTP Range，只提取目标成员，没有下载 8--34 GB 的整包
- 本地 raw 数据：`/home/kevin/datasets/CORE4D-Base/CORE4D_Real/`，位于 Git worktree 之外

## 文件清单

| 文件 | Bytes | SHA-256 |
|---|---:|---|
| `human_object_motions/20231003_2/000/person1_poses.npz` | 22,051,700 | `612d3fa034b9804e9da8bb76ce3cebfd15ae642f7b8e6714f8acaee754eae3c1` |
| `human_object_motions/20231003_2/000/person2_poses.npz` | 22,162,760 | `80f89de4b4e811ac6c9a3fc4d9d92d197c1692e81cfd48211e1335b7f5be3479` |
| `human_object_motions/20231003_2/000/smooth_objposes.npy` | 25,344 | `89c01f60808add83b6e961b01b21aa1692618705467097a011ca3b07bd634556` |
| `human_object_motions/20231003_2/000/object_metadata.json` | 112 | `0612b88b6cb33e5c2e81996686dc79404c680e7de15c2068668ef356c1519d81` |
| `human_object_motions/20231003_2/000/aligned_frame_ids.txt` | 4,653 | `84fa54893a5e01191798ec8485a875673d38200afade3ac6dfb6f0f01b58865e` |
| `object_models/desk/desk001_m.obj` | 166,565 | `f8f9dffa9f1ccdbd2ff90125cd39c96e79bf84ccebd8a81fcef656c4fbc7c00b` |

合计 44,411,134 bytes，约 42.35 MiB。

## 人体字段审计

两份 person NPZ 的顶层都只有 `arr_0`，内部是官方 V1 pickle 字典。两人字段一致：

| 字段 | 实测形状 | dtype | 结果 |
|---|---:|---|---|
| `betas` | `(197, 10)` | `float32` | 所有帧完全相同，有限 |
| `global_orient` | `(197, 3)` | `float32` | 有限 |
| `transl` | `(197, 3)` | `float32` | 有限 |
| `body_pose` | `(197, 21, 3)` | `float32` | 有限 |
| `left_hand_pose` | `(197, 12)` | `float32` | 有限 |
| `right_hand_pose` | `(197, 12)` | `float32` | 有限 |
| `joints` | `(197, 127, 3)` | `float32` | 有限 |
| `vertices` | `(197, 10475, 3)` | `float32` | 有限 |
| `expression` | `(197, 10)` | `float32` | 全零；adapter 不需要 |

当前 adapter 只读取 retarget 所需字段；忽略 `expression` 和人体 mesh 顶点不会改变 OmniRetarget 的输入契约。

## 物体与坐标审计

- `smooth_objposes.npy` 实测为 `(197, 4, 4)`、`float64`，全部有限。
- 所有齐次矩阵最后一行均严格为 `[0, 0, 0, 1]`。
- 最大旋转正交误差约 `9.99e-16`；行列式范围约 `[0.9999999999999991, 1.0000000000000007]`。
- 物体首尾平移差约 `[-1.037, -0.009, 2.383] m`；这是源坐标中的轨迹差，不在 raw audit 中解释为任务目标。
- mesh 有 2,414 个顶点和 5,000 个面；canonical bbox extents 约为 `[0.599, 0.731, 0.799] m`。
- 第 0 帧变换到世界系后，desk bbox 的高度范围约为 `y=[0.020, 0.753] m`。
- 两人的脚部关节 `10/11` 的世界 `y` 中位数分别约为 `0.011 m` 和 `0.018 m`，身体关节最大 `y` 分别约为 `1.663 m` 和 `1.671 m`。结合官方 benchmark 对高度轴的使用，实样本支持 **Y-up、单位米**。
- adapter 使用右手系 `Rx(+90°)` 将 `(x,y,z)` 映射为 `(x,-z,y)`，得到 Omni 的 Z-up；最终仍须用源动作可视化确认人物前向、左右身份和接触。

## 同步与接触注意事项

- `person1/person2/object/aligned_frame_ids` 的帧数为 `[197, 197, 197, 197]`，通过严格相等检查。
- `aligned_frame_ids.txt` 是 `(197, 6)` 整数表；首行为 `[0,20,57,39,432,428]`，末行为 `[201,221,257,239,1634,1619]`。
- 其中部分列不是严格单调。官方没有定义六列语义，因此 adapter 只原样保存，不用它重排、截断或拒绝 motion。
- 以腕关节到 canonical mesh AABB 的距离只能做粗略 sanity check，不能当作接触标签；手指/掌面、mesh 表面和动作可视化才是后续判断依据。

## V1/V2 决策

首轮固定使用 V1 完整五文件，因为这是官方 file definition、可视化程序和 benchmark 直接定义的自洽 scene。V2 只有更新后的人体结果，顶层键和数据类型也不同；以后若要升级，应在同一序列上把 `V2 human + V1 scene` 作为单独对照，确认帧、身份、坐标和接触时序后再采用。

## Stage A 状态与下一步

- [x] 确定动作与物体候选
- [x] 只提取最小官方文件集
- [x] 保存 SHA-256、字段、shape、dtype 和数值审计
- [x] 验证 197 帧严格同步、15 FPS、Y-up 和 mesh 解析
- [x] 运行 adapter，生成纯数值、`allow_pickle=False` 的 paired NPZ
- [x] 在 ViSER 同时显示两个人的 joint/link 骨架、desk、地面和腕坐标轴；人工确认源动作整体合理

前四项构成 Stage A raw audit；后两项属于 Stage B。2026-08-31 的人工检查接受了当前 joint/link 源动作显示，未发现需要阻止进入 OmniRetarget 的人物布局或桌子 pose 问题。该验收不等于已经验证手掌 mesh 接触，也不等于机器人重定向完成。
